import 'package:flutter/material.dart';

/// Flutter code sample for [Scaffold].

void main() {
  var app = MaterialApp(
      home: Scaffold(
          appBar: AppBar(
    title: const Text("Hola mundo"),
  )));

  runApp(app);
}
